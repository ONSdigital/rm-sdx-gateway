info:
  contactEmail: philippe.brossier@ons.gov.uk
  version: ${project.version}
  commit: ${buildNumber}
  branch: ${scmBranch}
  buildTime: ${timestamp}

management:
  port: 8291
  context-path: /mgmt

spring:
  application:
    name: Survey Data Exchange Gateway
