<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-sftp="http://www.springframework.org/schema/integration/sftp"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
  http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
  http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
  http://www.springframework.org/schema/integration/sftp
  http://www.springframework.org/schema/integration/sftp/spring-integration-sftp.xsd">
  
    <bean id="sftpSessionFactory" class="org.springframework.integration.file.remote.session.CachingSessionFactory">
        <constructor-arg ref="defaultSftpSessionFactory" />
        <constructor-arg value="1"/>
        <property name="sessionWaitTimeout" value="5000"/>
    </bean>

    <bean id="defaultSftpSessionFactory" class="org.springframework.integration.sftp.session.DefaultSftpSessionFactory">
        <property name="host" value="${sftp.host}"/>
        <property name="port" value="${sftp.port}"/>
        <property name="user" value="${sftp.username}"/>
        <property name="password" value="${sftp.password}"/>
        <property name="allowUnknownKeys" value="true"/>
    </bean>

    <int:channel id="sftpInbound"/> <!--  To Java -->
    <int:channel id="sftpOutbound"/>
    <int:channel id="sftpStreamTransformer"/>
    
    <int-sftp:inbound-streaming-channel-adapter id="sftpInboundAdapter"
            channel="sftpInbound"
            session-factory="sftpSessionFactory"
            filename-pattern="Receipt*.txt"
            remote-file-separator="/"
            remote-directory="${sftp.directory}"
            >
		<int:poller cron="${sftp.cron}"> 
       	<int:advice-chain>
       		<bean class="uk.gov.ons.ctp.sdx.service.impl.SftpAdviceInterceptor" />
       	</int:advice-chain>
        </int:poller>
    </int-sftp:inbound-streaming-channel-adapter>
      
    <int:stream-transformer input-channel="sftpStreamTransformer" output-channel="sftpOutbound"/>
                                       
	<int-sftp:outbound-gateway id="renameFile"
                              session-factory="sftpSessionFactory"
                              request-channel="sftpOutbound"
                              reply-channel="nullChannel"
                              auto-startup="true"
                              remote-file-separator="/"
                              command="mv"
                              expression="headers['file_remoteDirectory'] + '/' + headers['file_remoteFile']"
                              rename-expression="headers['file_remoteDirectory'] + '/' + headers['file_remoteFile'] + '.processed'"
                              order="1">
    </int-sftp:outbound-gateway>
</beans>